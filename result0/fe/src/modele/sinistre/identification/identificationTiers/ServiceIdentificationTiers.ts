import axios from 'axios';
import { API_URL } from 'commun';

const resourceUri = API_URL + '/identificationTiers';

const ServiceIdentificationTiers = {
};

export default ServiceIdentificationTiers;