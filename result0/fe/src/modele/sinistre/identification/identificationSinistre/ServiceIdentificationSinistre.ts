import axios from 'axios';
import { API_URL } from 'commun';

const resourceUri = API_URL + '/identificationSinistre';

const ServiceIdentificationSinistre = {
};

export default ServiceIdentificationSinistre;