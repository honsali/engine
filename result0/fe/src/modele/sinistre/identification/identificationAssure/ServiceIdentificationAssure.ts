import axios from 'axios';
import { API_URL } from 'commun';

const resourceUri = API_URL + '/identificationAssure';

const ServiceIdentificationAssure = {
};

export default ServiceIdentificationAssure;